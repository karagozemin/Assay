# Assay agent — the spending brain.
# Kept intentionally light: the embedding is offline/deterministic (see assay/embed.py),
# so no external embedding keys are required and every run is reproducible.
langgraph>=0.2.0
langchain-core>=0.3.0
httpx>=0.27.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
typer>=0.12.0
rich>=13.7.0
