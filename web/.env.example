# Backend (registry + ledger + metrics) base URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000
# Agent SSE API base URL (POST /run streams the ASSAY decisions)
NEXT_PUBLIC_AGENT_URL=http://localhost:8000
